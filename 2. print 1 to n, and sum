#include <stdio.h>

int main()
{
    int n=1, sum=0;
    int new;

    scanf("%d", &new);
    while (n <= new){
        printf("%d ", n);
        sum = sum + n;
        n++;
    }
    printf("%d", sum);
    return 0;
}
