#include <stdio.h>

int main()
{
    int n;
    scanf("%d", &n);
    
    int a=1;
    while(a<=n) {
        if (n%a==0) {
            printf("%d ", a);
            a++;}
        else a++;
    }
    return 0;
}
